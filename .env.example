# Supabase (server-only: never expose these to the client)
# Project URL (same as NEXT_PUBLIC_SUPABASE_URL in front, but no NEXT_PUBLIC_ prefix)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Storage bucket for task attachments (mentor) and submission files (student). Create in Dashboard → Storage, set Public.
SUPABASE_TASK_BUCKET=task-files

# JWT signing/verification (e.g. for session or webhook verification)
JWT_SECRET=your-jwt-secret

# Custom auth: RS256 JWT keys (run: python scripts/gen_jwt_keys.py)
# Paste private key below; use \n for newlines in .env, or put on one line.
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# CORS allowed origins (comma-separated). Default: http://localhost:3000,https://solstudy.vercel.app
# ALLOWED_ORIGINS=https://solstudy.vercel.app,http://localhost:3000
